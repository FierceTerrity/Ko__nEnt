import { AxiosRequestConfig, AxiosResponse } from 'axios';
export declare abstract class Client {
    private apiKey;
    constructor(config: {
        apiKey: string;
    });
    protected request<T>(endpoint: string, options?: AxiosRequestConfig): Promise<AxiosResponse<any, any>>;
}
