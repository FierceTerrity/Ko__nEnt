"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.Bans = void 0;
const base_1 = require("../../base");
const qs_1 = __importDefault(require("qs"));
const resourceName = 'bans';
class Bans extends base_1.Client {
    //CreateBan to be implemented
    //Ban Import/Export to be implemented
    deleteBanById(banId) {
        return this.request(`${resourceName}/${banId}`, { method: 'DELETE' });
    }
    getBanInfoById(banId, params) {
        let query = `${resourceName}/${banId}?`;
        if (params) {
            query += qs_1.default.stringify(params, { encode: false, arrayFormat: 'comma' });
        }
        return this.request(query);
    }
    getBans(filter, params) {
        let query = `${resourceName}?`;
        if (filter) {
            query +=
                qs_1.default.stringify({ filter }, { encode: false, arrayFormat: 'comma' }) + '&';
        }
        if (params) {
            query += qs_1.default.stringify(params, { encode: false, arrayFormat: 'comma' });
        }
        console.log(query);
        return this.request(query);
    }
}
exports.Bans = Bans;
