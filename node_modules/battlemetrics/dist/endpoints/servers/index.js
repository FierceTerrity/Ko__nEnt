"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.Servers = void 0;
const base_1 = require("../../base");
const resourceName = 'servers';
class Servers extends base_1.Client {
    runRawCommand(cmd, serverId) {
        let query = `${resourceName}/${serverId}/command`;
        let rawCommandObj = {
            data: {
                attributes: {
                    command: 'raw',
                    options: {
                        raw: cmd,
                    },
                },
                type: 'rconCommand',
            },
        };
        return this.request(query, {
            method: 'POST',
            data: JSON.stringify(rawCommandObj),
        });
    }
}
exports.Servers = Servers;
